server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # SPA fallback so deep links like /join?token=... and /session/:id work.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
